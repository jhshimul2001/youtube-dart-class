void main() {
  //arithmatic operator

  // int a = 50;
  // int b = 30;
  // print(a + b);
  // print(a / b);
  // print(a ~/ b);
  // print(a % b);

  // releational operator

  // int c = 10;
  // int d = 20;
  // print(c > d);

  //type test operator

  // int s = 20;
  // String name = 'shimul';
  // print(s is int);
  // print(name is! String);

  //assignment operator

  // int k = 10;
  // int z = 5;
  // k -= z; // k = k+z;
  // print(k);

  //conditional operator

  // var a = 12;
  // var res = a <= 12 ? 'value greather than 12' : 'value less than 12';
  // print(res);

  // logical operator => and, or, not

  int h = 25;
  int l = 30;
  bool result = (h < l) && (h > l); // boolean  return kore true or false
  print(result);
  bool res = (h < l) || (h > l);
  print(res);
  bool r = !(h <= 25);
  print(r);
}
