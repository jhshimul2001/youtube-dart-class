void main() {
  // int age = 30;
  // print(age);

  // double a = 2.0;
  // print(a);

  // String name = 'shimul';
  // print(name);

  // bool value = true;
  // print(value);

  // List mylist = ['one', 'two', 'three', 22];
  // print(mylist);

  // Map mymap = {'name': 'shimul', 'age': 22, 'id': 201005};
  // print(mymap);

  Set myset = {'one', 'one', 'two'};
  print(myset);
}
